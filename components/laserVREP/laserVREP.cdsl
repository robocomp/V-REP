import "Laser.idsl";

Component laserVREP
{
    Communications
    {
        implements Laser;
        //subscribesTo topicToSubscribeTo;
        //publishes topicToPublish;
    };
	language python;
	//options agmagent;
	//options InnerModelViewer;
};
